[tool.poetry]
name = "delta-spark"
version = "1.3.0.dev"
description = ""
authors = ["Your Name <you@example.com>"]

packages = [
    { include = "delta", from = "python" },
]

[tool.poetry.dependencies]
python = "^3.6"
pyspark = ">=3.2.0,<3.3.0"
importlib-metadata = ">=1.0.0,<4.0"

[tool.poetry.dev-dependencies]
mypy = "0.910"
chispa = "^0.9.2"
pytest = "^6.0"
pytest-spark = "^0.6.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
